<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yjo.book">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- network 상태를 미리 체크하여 연결 안된 경우 다이얼로그를 보여주려면 아래 permission 추가 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- android test 돌릴 때 화면 lock 풀어야 로그인 touch 가능 -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".test5" />
        <activity android:name=".chat_list" />
        <activity android:name=".chat" />
        <activity android:name=".Comcoment_update" />
        <activity android:name=".Comment_update" />
        <activity android:name=".Post_update" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".SinUp2" />
        <activity android:name=".test" />
        <activity android:name=".main_view" />
        <activity android:name=".test2" />
        <activity android:name=".Searchfriend" />
        <activity android:name=".profile" />
        <activity android:name=".post_new" />
        <activity android:name=".Myprofile" />
        <activity
            android:name=".Comment"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".Comcomment" />
        <activity android:name=".good_friend_list" />
        <activity android:name=".Friend_list" />

        <service android:name=".Sevrice_back" />
    </application>

</manifest>